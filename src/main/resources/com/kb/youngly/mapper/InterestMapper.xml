<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kb.youngly.mapper.InterestMapper">

    <select id="findUserInterestsByInvestment"
            resultType="com.kb.youngly.vo.survey.InterestVO">
        SELECT
            i.interest_id,
            i.interest_name,
            i.is_investment,
            i.created_at
        FROM interest_users iu
                 INNER JOIN interests i
                            ON i.interest_id = iu.interest_id
        WHERE iu.user_id = #{userId}
          AND i.is_investment = #{investment}
        ORDER BY i.interest_id ASC
    </select>
</mapper>
