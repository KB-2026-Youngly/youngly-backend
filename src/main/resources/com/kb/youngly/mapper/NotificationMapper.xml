<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kb.youngly.mapper.NotificationMapper">

    <select id="findNotifications"
            resultType="com.kb.youngly.dto.notification.NotificationResponse">

        SELECT
            notification_id,
            notification_type,
            content,
            is_read,
            created_at
        FROM notifications
        WHERE user_id = #{userId}
          AND is_read = FALSE
        ORDER BY created_at DESC

    </select>

    <select id="findNotificationById"
            resultType="com.kb.youngly.vo.user.NotificationVO">

        SELECT
            notification_id,
            user_id,
            notification_type,
            content,
            is_read,
            created_at
        FROM notifications
        WHERE notification_id = #{notificationId}

    </select>

    <update id="readNotification">

        UPDATE notifications
        SET is_read = TRUE
        WHERE notification_id = #{notificationId}

    </update>

    <delete id="deleteNotification">

        DELETE
        FROM notifications
        WHERE notification_id = #{notificationId}

    </delete>

    <insert id="insertNotification"
            useGeneratedKeys="true"
            keyProperty="notificationId"
            parameterType="com.kb.youngly.vo.user.NotificationVO">

        INSERT INTO notifications(
            user_id,
            notification_type,
            content
        )
        VALUES(
                  #{userId},
                  #{notificationType},
                  #{content}
              )

    </insert>

</mapper>